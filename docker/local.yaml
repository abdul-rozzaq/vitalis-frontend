services:
  vitalis-frontend:
    image: registry.solara.uz/ci-user/vitalis-frontend:latest
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: vitalis-frontend
    env_file:
      - ../.env
    ports:
      - "3000:3000"

    restart: unless-stopped
